services:
  db:
    image: mysql:8.0
    container_name: mysql-portal
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: portal
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: